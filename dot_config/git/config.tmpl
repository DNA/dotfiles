[user]
	name = {{ .name | quote }}
	email = {{ .email | quote }}

[diff]
	colorMoved = plain

{{- if .isWork }}
[credential]
    helper =
    helper = /usr/local/share/gcm-core/git-credential-manager

[credential "https://dev.azure.com"]
       useHttpPath = true
{{ end }}
