# This config runs on any interactive shell
#
# This is the main place for interactive configuration, and
# where probably most of your scripts will be defined.
#

# Auto load configs
autoload -U compinit; compinit

# Define keybindings
bindkey -e                              # Start with Emacs pattern
bindkey "^[[1;3D" backward-word         # Define opt + <-
bindkey "^[[1;3C" forward-word          # Define opt + ->
bindkey "^[[1;4D" beginning-of-line     # Define opt + <-
bindkey "^[[1;4C" end-of-line           # Define opt + <-

# Initialize shell applications
command -v brew     &>/dev/null && eval "$(brew shellenv)"
command -v mise     &>/dev/null && eval "$(mise activate zsh)"
command -v starship &>/dev/null && eval "$(starship init zsh)"

alias ll="ls -lha --color"
